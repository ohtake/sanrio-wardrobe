---
version: "2"
plugins:
  duplication:
    enabled: true
    config:
      languages:
      - javascript
  eslint:
    enabled: true
    channel: eslint-4
    config:
      config: .eslintrc.yaml
  fixme:
    enabled: true
exclude_patterns:
- test/
